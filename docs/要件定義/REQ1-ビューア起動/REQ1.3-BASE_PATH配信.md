# REQ1.3: BASE_PATH 配信

[R6.2 — サブパス配信](../../要求定義/R6-デプロイ柔軟性/R6.2-サブパス配信.md) を満たすため、サブパス配下で動作させるための設定を提供する。

## 要件

- 環境変数 `BASE_PATH` でサブパス（例: `/wiki`）を指定できる
- サブパス配下でもアセット・API パス・クライアント側のフェッチが正しく解決される
- `BASE_PATH` 未指定時はルート直下で動作する（後方互換）
- 指定値を変えて起動した際には、成果物を再構成して反映する（再ビルドが必要な場合でも自動で実行する）
