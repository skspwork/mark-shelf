# REQ1.1: npx 起動

[R1.1 — 独立ビューア](../../要求定義/R1-ドキュメント閲覧性/R1.1-独立ビューア.md) を満たすため、対象リポジトリのルートで `npx markshelf` を実行するとビューアが起動する。

## 要件

- グローバルインストール不要
- 設定ファイル不要
- 起動後、ブラウザで閲覧できる URL を標準出力に表示する

## 関連用語

- npx
